node_modules
.dist
/dist
.DS_Store
npm-debug.log*
yarn-error.log

# Environment / secrets — never commit these
.env
.env.local
.env*.local
server/.env
*.pem
*.key

# Local database artifacts — never commit user/test data
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Playwright artifacts (may contain URLs, session data, screenshots)
.playwright-mcp/
test-results/
playwright-report/
*.png
*.jpeg
*.jpg

# Build / cache
.vite/
